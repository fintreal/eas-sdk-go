name: Release

on:
  push:
    branches:
      - main

jobs:
  Test:
    runs-on: ubuntu-latest
    env:
      EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
      EXPO_ME_ID: ${{ vars.EXPO_ME_ID }}
      EXPO_ACCOUNT_NAME: ${{ vars.EXPO_ACCOUNT_NAME }}
      EXPO_ACCOUNT_ID: ${{ vars.EXPO_ACCOUNT_ID }}
      EXPO_TEST_GET_APP_ID: ${{ vars.EXPO_TEST_GET_APP_ID }}
      EXPO_TEST_UPDATE_APP_ID: ${{ vars.EXPO_TEST_UPDATE_APP_ID }}
      EXPO_TEST_UPDATE_ENVIRONMENT_VARIABLE_ID: ${{ vars.EXPO_TEST_UPDATE_ENVIRONMENT_VARIABLE_ID }}
      EXPO_TEST_GET_ENVIRONMENT_VARIABLE_ID: ${{ vars.EXPO_TEST_GET_ENVIRONMENT_VARIABLE_ID }}
      EXPO_TEST_GET_APPLE_TEAM_ID: ${{ vars.EXPO_TEST_GET_APPLE_TEAM_ID }}
      EXPO_TEST_GET_APPLE_TEAM_NAME: ${{ vars.EXPO_TEST_GET_APPLE_TEAM_NAME }}
      EXPO_TEST_GET_APPLE_TEAM_IDENTIFIER: ${{ vars.EXPO_TEST_GET_APPLE_TEAM_IDENTIFIER }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.24.1'

      - name: Run tests
        run: go test ./... -count=1 -cover

  Release:
    runs-on: ubuntu-latest
    needs: [Test]
    permissions:
      contents: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Get next version
        id: semver
        uses: ietf-tools/semver-action@v1.10.1
        with:
          token: ${{ github.token }}
          branch: main

      - name: Create release
        uses: ncipollo/release-action@v1.16.0
        with:
          makeLatest: true
          name: ${{ steps.semver.outputs.next }}
          tag: ${{ steps.semver.outputs.next }}
          body: Changelog
          token: ${{ github.token }}
